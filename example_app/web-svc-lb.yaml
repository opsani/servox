---
apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  type: LoadBalancer
  selector:
    app: web
  ports:
  - protocol: TCP
    port: 8080
    name: co-http

---
apiVersion: v1
kind: Service
metadata:
  name: web-preview
spec:
  type: LoadBalancer
  selector:
    app: web
  ports:
  - protocol: TCP
    port: 8080
    name: co-http
